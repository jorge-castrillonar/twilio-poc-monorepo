# ============================================
# Database Configuration
# ============================================

# Core Service Database
CORE_DB_USERNAME=core_user
CORE_DB_PASSWORD=core_pass

# S3 Service Database
S3_DB_USERNAME=s3_user
S3_DB_PASSWORD=s3_pass

# User Service Database
USER_DB_USERNAME=user_user
USER_DB_PASSWORD=user_pass

# Auth Service Database
AUTH_DB_USERNAME=auth_user
AUTH_DB_PASSWORD=auth_pass

# ============================================
# JWT Configuration
# ============================================
# Generate keys using: cd auth-service/keys && ./generate-keys-and-jwt.sh
# Then encode to Base64 and paste here

JWT_PUBLIC_KEY=your-base64-encoded-public-key-here
JWT_PRIVATE_KEY=your-base64-encoded-private-key-here

# ============================================
# Redis Configuration (for rate limiting)
# ============================================

REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# ============================================
# ActiveMQ Configuration
# ============================================

ACTIVEMQ_BROKER_URL=tcp://activemq:61616
ACTIVEMQ_USER=admin
ACTIVEMQ_PASSWORD=admin

# ============================================
# Service URLs (Docker network)
# ============================================

CORE_SERVICE_URL=http://core-service:8081
S3_SERVICE_URL=http://s3-service:8080
USER_SERVICE_URL=http://user-service:8084
AUTH_SERVICE_URL=http://auth-service:8085

# ============================================
# Spring Profiles
# ============================================

SPRING_PROFILES_ACTIVE=docker

# ============================================
# IMPORTANT NOTES:
# ============================================
# 1. Copy this file to .env: cp .env.example .env
# 2. NEVER commit .env to git (already in .gitignore)
# 3. Generate JWT keys before first run:
#    cd auth-service/keys
#    ./generate-keys-and-jwt.sh
#    cat public_key_base64.txt  # Copy to JWT_PUBLIC_KEY
#    cat private_key.pem | base64 -w 0  # Copy to JWT_PRIVATE_KEY
# 4. Change default passwords in production!
# 5. Use secrets manager in production (AWS Secrets Manager, Vault, etc.)
